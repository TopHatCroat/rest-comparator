import loadDatabase from "./database";

import {
    CREATE_STATEMENT,
    INSERT_STATEMENT,
    newRequests,
} from "./operations";

export {
    CREATE_STATEMENT,
    INSERT_STATEMENT,
    newRequests,
};

export default loadDatabase;
